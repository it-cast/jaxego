---
phase: 06-rea-oper-vel-bairros-config-cobertura-e-tabela-de-frete
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/tests/neighborhoods/__init__.py
  - apps/api/tests/neighborhoods/test_models.py
  - apps/api/tests/neighborhoods/test_spatial.py
  - apps/api/tests/neighborhoods/conftest.py
  - apps/api/tests/couriers/test_coverage.py
  - apps/api/tests/couriers/test_pricing_floor.py
  - apps/api/tests/couriers/test_availability.py
  - apps/api/tests/areas/__init__.py
  - apps/api/tests/areas/test_config_audit.py
  - apps/api/alembic/versions/0005_area_operable.py
  - apps/api/app/areas/config_schema.py
  - apps/api/app/areas/schemas.py
  - apps/api/app/areas/service.py
  - apps/api/app/areas/router.py
autonomous: true
requirements: [REQ-002, REQ-003, REQ-016, REQ-017, REQ-018]
must_haves:
  truths:
    - "Migration 0005 cria neighborhoods_catalog (POLYGON nullable SRID 4326), courier_coverage_areas e courier_pricing_tables reusando convenções 0004 (utf8mb4, FK RESTRICT, DATETIME(6))"
    - "couriers ganha is_online, max_concurrent (online/offline; busy derivado depois)"
    - "Area.config sensível passa por Pydantic tipado (AreaConfig) com ranges; mudança sensível grava before/after em audit_log"
    - "Test scaffold de toda a Phase 6 existe e os módulos de teste importam sem erro (Wave 0)"
  artifacts:
    - path: "apps/api/alembic/versions/0005_area_operable.py"
      provides: "Schema espacial + relacional da Phase 6"
      contains: "neighborhoods_catalog"
    - path: "apps/api/app/areas/config_schema.py"
      provides: "Pydantic AreaConfig tipada com ranges + chaves sensíveis"
      contains: "class AreaConfig"
    - path: "apps/api/tests/neighborhoods/test_spatial.py"
      provides: "Scaffold do teste espacial @pytest.mark.mysql (ST_Contains dentro/fora)"
      contains: "pytest.mark.mysql"
  key_links:
    - from: "apps/api/app/areas/service.py"
      to: "audit_log"
      via: "write_audit em update de config sensível"
      pattern: "write_audit"
    - from: "apps/api/app/areas/service.py"
      to: "AreaConfig"
      via: "validação tipada antes de gravar config"
      pattern: "AreaConfig"
---

<objective>
Estabelecer a fundação backend da Phase 6: o **test scaffold (Wave 0)** de toda a fase, a **migration 0005** (3 tabelas novas + extensão de `couriers`) e a **config tipada da área** (`AreaConfig` Pydantic) com **audit** em mudança sensível — substituindo o JSON cru que `update_area` grava hoje.

Purpose: as plans 02–05 dependem deste schema e dos testes-fantasma já criados. Sem a migration e os modelos, nada espacial roda; sem o AreaConfig, piso/geofence/timeouts entram sem validação nem trilha de auditoria (Pitfall 4 do RESEARCH, RN-012/F-08 E2).
Output: migration 0005, `AreaConfig`, `update_area` validado+auditado, e os arquivos de teste-fantasma de neighborhoods/couriers/areas.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-rea-oper-vel-bairros-config-cobertura-e-tabela-de-frete/06-CONTEXT.md
@.planning/phases/06-rea-oper-vel-bairros-config-cobertura-e-tabela-de-frete/RESEARCH.md

# Convenções de migration e schema a reusar
@apps/api/alembic/versions/0004_couriers_kyc.py
@apps/api/app/db/mixins.py
@apps/api/app/areas/models.py
@apps/api/app/areas/service.py
@apps/api/app/areas/router.py
@apps/api/app/audit/service.py
@apps/api/tests/conftest.py

<interfaces>
<!-- Contratos já existentes que este plano consome. Não explorar o codebase. -->

De apps/api/app/db/mixins.py:
```python
UTC_DATETIME = DateTime(timezone=True).with_variant(MYSQL_DATETIME(fsp=6), "mysql")
BIG_ID = BigInteger().with_variant(Integer, "sqlite")
class TimestampMixin: ...        # created_at / updated_at DATETIME(6) aware-UTC (TD-010)
class AreaScopedMixin: ...       # area_id BIGINT NOT NULL + index + FK RESTRICT -> areas
```

De apps/api/app/audit/service.py:
```python
async def write_audit(session, *, actor_id, action, area_id=None,
                      before=None, after=None, ip=None, cross_area_bypass=False) -> AuditLog
```

De apps/api/app/areas/service.py (a ESTENDER):
```python
async def update_area(session, area_id, body: AreaUpdate) -> Area   # hoje grava body.config CRU (sem validar/auditar)
async def get_area(session, area_id) -> Area
```

De apps/api/alembic/versions/0004 (convenção a copiar):
```python
_TABLE_KW = {"mysql_charset": "utf8mb4", "mysql_collate": "utf8mb4_unicode_ci"}
def _dt() -> sa.types.TypeEngine:  # DATETIME(6) MySQL / DateTime SQLite
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (Wave 0): Test scaffold de toda a Phase 6</name>
  <files>apps/api/tests/neighborhoods/__init__.py, apps/api/tests/neighborhoods/conftest.py, apps/api/tests/neighborhoods/test_models.py, apps/api/tests/neighborhoods/test_spatial.py, apps/api/tests/couriers/test_coverage.py, apps/api/tests/couriers/test_pricing_floor.py, apps/api/tests/couriers/test_availability.py, apps/api/tests/areas/__init__.py, apps/api/tests/areas/test_config_audit.py</files>
  <skills_applied>quality/observability-production (testabilidade), domain/fastapi-production-patterns (estrutura de teste), webapp-testing</skills_applied>
  <action>
    Criar os arquivos-fantasma do "Wave 0 Gaps" do RESEARCH. Cada um com testes reais (não `pass`) descrevendo o comportamento esperado das plans 02/03 — eles FALHAM agora (vermelho) e ficam verdes quando a feature chegar. NÃO criar a feature aqui.
    - `tests/neighborhoods/conftest.py`: fixtures de bairro de teste — um WKT/GeoJSON de polígono pequeno conhecido (ex.: quadrado em torno de uma lat/lng de Pádua) + um ponto DENTRO e um ponto FORA desse polígono, expostos como constantes/fixtures reutilizáveis.
    - `tests/neighborhoods/test_models.py` (SQLite): REQ-003 — criar bairro só por NOME (sem polígono) persiste e lê (area-scoped). Marcar `xfail`/import-guard tolerante se o módulo `app.neighborhoods` ainda não existir, OU criar o teste de modo que só falhe na asserção depois que a Plan 02 criar o módulo (preferir: import dentro do teste para o scaffold coletar sem ImportError fatal — usar `pytest.importorskip("app.neighborhoods.models")`).
    - `tests/neighborhoods/test_spatial.py`: `pytestmark = pytest.mark.mysql`. REQ-003 — ponto DENTRO do polígono → elegível; ponto FORA → não elegível, via helper `point_in_polygon` da Plan 02 (`pytest.importorskip`). Documenta no docstring: "requer MySQL 8 real (ST_Contains); skipado fora do CI MySQL — padrão Phase 5".
    - `tests/couriers/test_coverage.py` (SQLite): REQ-016 — elegibilidade exige cobertura nos DOIS pontos (coleta E entrega) e exclusão veta os dois (Pattern 3 do RESEARCH). `importorskip("app.couriers.coverage")`.
    - `tests/couriers/test_pricing_floor.py` (SQLite): REQ-017 — preço < piso → erro 422 cuja mensagem CITA o piso (modo `neighborhood` valida `piso_entrega`; modo `km` valida `piso_km`). `importorskip("app.couriers.pricing")`.
    - `tests/couriers/test_availability.py` (SQLite): REQ-018 — só courier `active` fica online; `busy` é derivado (não persistido como campo editável). `importorskip("app.couriers.availability")`.
    - `tests/areas/__init__.py` + `tests/areas/test_config_audit.py` (SQLite): REQ-002 — alterar chave sensível da config (piso/geofence/kyc_level) grava 1 linha em `audit_log` com before/after; alterar chave não-sensível NÃO gera audit duplicado. Este teste fica verde já com a Task 3 deste plano.
    - Confirmar que o marker `mysql` está registrado em `apps/api/pyproject.toml` (já usado na Phase 5); se ausente, adicionar `markers = ["mysql: requires live MySQL 8", "asyncio: ..."]` sob `[tool.pytest.ini_options]`.
  </action>
  <verify>
    <automated>cd apps/api && uv run pytest tests/areas/test_config_audit.py tests/neighborhoods tests/couriers/test_coverage.py tests/couriers/test_pricing_floor.py tests/couriers/test_availability.py --co -q</automated>
  </verify>
  <done>Os arquivos de teste existem e são COLETÁVEIS sem ImportError (importorskip protege módulos ainda inexistentes); `test_config_audit.py` é executável. Marker `mysql` registrado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (Wave 1): Migration 0005 — schema espacial + relacional + extensão de couriers</name>
  <files>apps/api/alembic/versions/0005_area_operable.py</files>
  <behavior>
    - `alembic upgrade head` cria as 3 tabelas + colunas novas sem erro (MySQL); `downgrade` reverte limpo.
    - SQLite (testes) ignora tipos espaciais — coluna `polygon` declarada via DDL bruto só no branch MySQL; SQLite recebe variant benigno (TEXT/sem coluna espacial) para `create_all` não quebrar (Pitfall 3).
  </behavior>
  <skills_applied>domain/mysql-schema-design (spatial POINT/POLYGON, SRID 4326, SPATIAL INDEX, FK RESTRICT, utf8mb4), domain/fastapi-production-patterns</skills_applied>
  <action>
    Criar `0005_area_operable.py` revisando `down_revision = "0004_couriers_kyc"`, copiando `_TABLE_KW` e `_dt()` de 0004. Reusar convenções: utf8mb4, FK RESTRICT/RESTRICT, índices `ix_*` em FKs, DATETIME(6) via `_dt()`.

    1) **neighborhoods_catalog** (AREA-SCOPED): `id` BIGINT PK; `area_id` BIGINT NOT NULL FK areas RESTRICT + index; `name` VARCHAR(120) NOT NULL; `is_informal` BOOLEAN NOT NULL default false (D-01 bairro informal); `archived_at` DATETIME(6) nullable (remoção é arquivo — bloqueio com entregas ativas é regra de serviço da Plan 02); `created_at`/`updated_at` `_dt()`. Index `(area_id, name)`. **Coluna espacial via DDL bruto MySQL-only** após `create_table`:
       ```python
       if op.get_bind().dialect.name == "mysql":
           op.execute("ALTER TABLE neighborhoods_catalog ADD COLUMN polygon POLYGON NULL SRID 4326")
       ```
       **LOW-4 / Pitfall 1 — SPATIAL INDEX exige NOT NULL:** polígono é NULLABLE (opcional M1), logo NÃO criar SPATIAL INDEX nesta coluna agora. Registrar como TECH-DEBT (ver Task 4 do plano não — registrar TD diretamente nesta migration via comentário + a Plan retorna a TD). Documentar no docstring da migration: "sem SPATIAL INDEX no M1 (Pádua pequena, full scan barato — TD post_launch_quarter; reavaliar >100 bairros/área)".

    2) **courier_coverage_areas** (AREA-SCOPED): `id` PK; `area_id` FK; `courier_id` BIGINT NOT NULL FK couriers RESTRICT + index; `neighborhood_id` BIGINT NOT NULL FK neighborhoods_catalog RESTRICT + index; `kind` VARCHAR(8) NOT NULL ('include'|'exclude' — Pattern 3 RN-003); timestamps. UNIQUE `(courier_id, neighborhood_id)` (uma linha por bairro por entregador).

    3) **courier_pricing_tables** (AREA-SCOPED): `id` PK; `area_id` FK; `courier_id` FK couriers RESTRICT + index; `mode` VARCHAR(16) NOT NULL ('neighborhood'|'km' — A3 RESEARCH); `neighborhood_id` BIGINT NULL FK (preenchido só no mode neighborhood); `up_to_km` DECIMAL/Numeric NULL (só mode km); `price` Numeric(10,2) NOT NULL (R$, centavos via app); `return_pct` Numeric(5,2) NULL (% retorno); timestamps. Index `(courier_id, mode)`.

    4) **couriers** (ALTER): adicionar `is_online` BOOLEAN NOT NULL default false; `max_concurrent` Integer NOT NULL default 1 (D-06; `busy` é DERIVADO — NÃO criar coluna busy). Usar `op.add_column`. No `downgrade`, `op.drop_column`.

    Usar `Numeric` (não Float) para dinheiro/percentual. `downgrade()` reverte tudo na ordem inversa (drop tables, drop columns).
  </action>
  <verify>
    <automated>cd apps/api && uv run python -c "import ast,sys; ast.parse(open('alembic/versions/0005_area_operable.py').read()); print('parse ok')" && uv run alembic upgrade head --sql > /dev/null && echo "offline-sql ok"</automated>
  </verify>
  <done>Migration parseia e gera SQL offline sem erro; `down_revision` aponta para 0004; 3 tabelas + 2 colunas em couriers; polígono via DDL MySQL-only; sem SPATIAL INDEX em coluna nullable (TD registrada); dinheiro em Numeric.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (Wave 1): AreaConfig tipada + audit em config sensível</name>
  <files>apps/api/app/areas/config_schema.py, apps/api/app/areas/schemas.py, apps/api/app/areas/service.py, apps/api/app/areas/router.py</files>
  <behavior>
    - Submeter config válida → persiste; chave fora do range (geofence 30–300, timeout_oferta 10–60, etc.) → 422 RFC-7807.
    - Alterar chave SENSÍVEL (piso_km, piso_entrega, geofence_m, kyc_level, timeout_oferta_s, timeout_favoritos_s, politica_retorno_pct) → 1 linha em audit_log com before/after dessas chaves.
    - Alterar só `name` (não sensível) → sem audit de config.
    (Coberto por tests/areas/test_config_audit.py da Task 1.)
  </behavior>
  <skills_applied>domain/fastapi-production-patterns (Pydantic v2 ranges), product/api-design-contracts (RFC-7807), quality/observability-production (audit como dado), owasp-security (validação de input numérico — ranges)</skills_applied>
  <action>
    1) `config_schema.py`: `class AreaConfig(BaseModel)` (Pydantic v2) com campos e bounds explícitos (Security Note 4 do RESEARCH):
       `kyc_level: Literal["simples","completa"]`; `piso_entrega: Decimal` (≥ 0); `piso_km: Decimal` (≥ 0); `geofence_m: int` (Field ge=30 le=300, default 80 — RN-005); `timeout_oferta_s: int` (ge=10 le=60, default 20 — ADR-104); `timeout_favoritos_s: int` (ge=30 le=180, default 60 — ADR-007); `politica_retorno_pct: int` (ge=0 le=100, default 0). `model_config = ConfigDict(extra="forbid")`. Expor `SENSITIVE_KEYS = frozenset({...})` com as 7 chaves auditáveis. Helper `diff_sensitive(before: dict, after: dict) -> tuple[dict, dict] | None` que retorna (before_subset, after_subset) só das chaves sensíveis que mudaram, ou None.
    2) `schemas.py`: `AreaUpdate.config` passa a ser validado como `AreaConfig | None` (parse tipado); manter compat com config parcial — decidir: aceitar config completa no PATCH de config (recomendado). Adicionar erro de range mapeado a RFC-7807 pelo handler global existente.
    3) `service.py update_area`: antes de gravar, se `body.config` presente, validar via `AreaConfig(**body.config)`; computar `diff = diff_sensitive(area.config or {}, new_config)`; gravar `area.config = new_config.model_dump(mode="json")`; retornar também o diff para o router auditar. NÃO sobrescrever cru (corrige Pitfall 4).
    4) `router.py update_area`: quando `diff` não-nulo, além do `_audit_bypass(action="area.update")` já existente, gravar `write_audit(action="area.config.update", before=diff_before, after=diff_after)` (RN-012 / F-08 E2). Manter o padrão 404-não-403 de cross-area.
    Datetimes aware-UTC (TD-010) — já garantido por write_audit.
  </action>
  <verify>
    <automated>cd apps/api && uv run pytest tests/areas/test_config_audit.py -x -q && uv run ruff check app/areas</automated>
  </verify>
  <done>AreaConfig valida ranges (422 fora do range); config sensível alterada gera audit before/after; chave não-sensível não gera audit de config; ruff verde.</done>
</task>

</tasks>

## Skills Consultadas
- `domain/mysql-schema-design` — migration 0005: POLYGON SRID 4326 via DDL bruto MySQL-only (Pitfall 1/3), FK RESTRICT, utf8mb4, DATETIME(6) `_dt()`, Numeric para dinheiro, indices em FK; reusa convencoes de `0004_couriers_kyc.py`.
- `domain/fastapi-production-patterns` — `AreaConfig` Pydantic v2 com `extra="forbid"`; service/router thin; aware-UTC (TD-010).
- `product/api-design-contracts` — erro de range mapeado a RFC-7807 (padrao do projeto); contrato de `update_area`.
- `quality/observability-production` — audit before/after como DADO em `audit_log` (RN-012), nunca structlog; log `area.config.update` com `request_id`.
- `owasp-security` — citada de leve: validacao de input numerico (ranges geofence/timeouts/pisos) + RBAC de area (404 cross-area). Sem PII nova (has_pii:false); ASVS L1 input validation.
- `meta/orchestration-decision-tree` — decomposicao da fase em waves (fundacao backend isolada destrava plans 02-05 em paralelo); back-front candidato a parallel-hint.
- `webapp-testing` — Wave 0 scaffold com `@pytest.mark.mysql` (spatial) + SQLite (piso/config).
- `quality/senior-quality-bar` — Gate 8: sem N+1 em listagens (list_neighborhoods/set_coverage com query única + delete/insert em lote), binds parametrizados anti-injection no SQL espacial (`ST_GeomFromGeoJSON`/`ST_Contains` via parâmetros, nunca f-string), todo endpoint com decisão explícita de auth (require_role + AreaScopeDep, 404 cross-área), sem segredo no repo, validação server-side de input numérico (ranges geofence/timeouts/pisos) e GeoJSON (anti-DoS de vértices/área).

## Skills Dispensadas (com justificativa)
- Matriz UI completa (ui-ux-pro-max, accessibility-pro, component-library-governance, design-tokens-system, empty-states-polish, form-ux-mastery, error-ux-patterns, saas-dashboard-patterns, data-tables-ux, dark-mode-theming, ux-copywriting-ptbr, responsive-breakpoint-strategy, gesture-touch-patterns) — plano backend-only (migration + config + scaffold); UI nas plans 04/05.
- `br/brazilian-forms` — sem formulario UI (mascara monetaria e das plans 04/05).
- `domain/safe2pay-escrow-br` / `domain/saas-billing-canonical` — has_payments:false; sem billing/checkout/assinatura (CLAUDE.md S18 nao dispara).
- `ux-advanced/file-upload-ux` — sem upload (poligono entra por GeoJSON em texto).
- `ux-advanced/onboarding-patterns` / `ux-advanced/trust-safety-ux` — sem signup novo (couriers ja da Phase 5).
- `mobile/push-notifications-architecture` / `ux-advanced/chat-ux-patterns` — sem push/chat.
- `br/lgpd-compliance` — has_pii:false; nenhum dado pessoal novo.

## Performance budget
- Migration 0005: DDL unico; indices em FKs. LOW-4/Pitfall 1: `polygon` NULLABLE -> sem SPATIAL INDEX no M1 (full scan barato em Padua; TD-017 reavalia >100 bairros/area).
- `update_area`: 1 SELECT + 1 UPDATE + <=1 INSERT de audit; sem N+1.

## Integration contracts
- N/A (integration_check:false). Sem integracao externa nesta fase.

## Tech debt deste plano
- TD-017 (novo, post_launch_quarter): `neighborhoods_catalog.polygon` sem SPATIAL INDEX (coluna nullable — LOW-4/Pitfall 1); reavaliar >100 bairros/area com poligono.
- TD-010 (existente, pre_launch_high): naive datetime — toda escrita de timestamp aware-UTC; `write_audit` ja garante.


<security_validation_notes>
## Security & Validation Notes (RESEARCH §Security — 6 itens; sem PII/integração/pagamento nova)

> integration_check:false → **N/A** (sem Integration contracts). has_pii:false → sem LGPD nova. Threat model pesado NÃO necessário; abaixo o escopo real de segurança/validação desta fase, distribuído nas plans 01–03.

1. **Validação de GeoJSON / coordenadas (anti-DoS):** input não confiável; validar server-side ANTES do DB — tipo `Polygon`, anel fechado, sem self-intersection, limite de vértices (~2000) e de área; lat∈[-90,90], lng∈[-180,180]. → **Plan 02** (escrita de bairro).
2. **RBAC de área (admin só a sua):** `AreaScoped` + `resolve_role`; recurso de outra área → **404, não 403** (não vaza existência). → **Plan 01 (config), 02 (bairros), 03 (cobertura/preço/availability)**.
3. **Audit em config sensível:** piso_km/piso_entrega/geofence_m/kyc_level/timeouts/politica_retorno → `write_audit("area.config.update", before/after)`. → **Plan 01 (Task 3)** [implementado aqui].
4. **Validação de input numérico (ranges):** Pydantic v2 bounds — geofence 30–300, timeout_oferta 10–60, timeout_favoritos 30–180, politica_retorno 0–100, pisos ≥0, max_concurrent ≥1. → **Plan 01 (AreaConfig)** + **Plan 03 (max_concurrent)**.
5. **Sem PII nova:** bairros/polígonos não são PII; coords de loja já existem. Nada de LGPD nesta fase.
6. **Online/offline só para `active`:** validar no endpoint de availability. → **Plan 03**.
</security_validation_notes>

<observability_checklist>
## Observability checklist (quality/observability-production)
- `update_area` com config sensível: log estruturado `area.config.update` (sem valores de PII — não há) + `request_id`; o DADO before/after vai para `audit_log`, NUNCA para structlog (RN-012).
- Erro de range (422) logado com `request_id` e a chave infratora (sem vazar segredo — não há).
- Migration 0005: sem observabilidade runtime (DDL).
</observability_checklist>

<low_confidence_resolution>
## LOW confidence → task/TD (Regra 12)
- **LOW-1** (GeoAlchemy2 vs `func.ST_*` nativo): DECISÃO travada — usar `func.ST_*` nativo no M1 (sem GeoAlchemy2). Implementado na **Plan 02**. Se a equipe quiser o tipo ORM, vira spike. → registrado como decisão + a Plan 02 implementa nativo.
- **LOW-2** (testar spatial sem MySQL local): RESOLVIDO — `@pytest.mark.mysql` (Task 1 deste plano cria `test_spatial.py`); piso/cobertura-por-nome em SQLite.
- **LOW-4** (SPATIAL INDEX em polígono nullable): DECISÃO — M1 sem índice (Task 2) + **TD `post_launch_quarter`** (registrar TD-017: "neighborhoods_catalog.polygon sem SPATIAL INDEX — full scan barato no piloto Pádua; reavaliar >100 bairros/área com polígono").
</low_confidence_resolution>

<verification>
- `cd apps/api && uv run pytest tests/areas/test_config_audit.py -x` → verde.
- `uv run alembic upgrade head --sql` → gera SQL sem erro (parse de DDL).
- `uv run ruff check app/areas` → verde.
- Scaffold (Task 1) coletável sem ImportError.
</verification>

<success_criteria>
- Migration 0005 cria neighborhoods_catalog (polygon POLYGON NULL SRID 4326), courier_coverage_areas, courier_pricing_tables e estende couriers (is_online, max_concurrent), reusando convenções de 0004.
- AreaConfig tipada com ranges; config sensível alterada → audit before/after (RN-012 / F-08 E2).
- Test scaffold de toda a fase existe (Wave 0), com test_spatial.py @pytest.mark.mysql.
- TD-017 (SPATIAL INDEX nullable) registrada.
</success_criteria>

<output>
After completion, create `.planning/phases/06-rea-oper-vel-bairros-config-cobertura-e-tabela-de-frete/06-01-SUMMARY.md`
</output>
