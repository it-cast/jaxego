---
phase: 06-rea-oper-vel-bairros-config-cobertura-e-tabela-de-frete
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - apps/api/app/neighborhoods/__init__.py
  - apps/api/app/neighborhoods/models.py
  - apps/api/app/neighborhoods/schemas.py
  - apps/api/app/neighborhoods/spatial.py
  - apps/api/app/neighborhoods/service.py
  - apps/api/app/neighborhoods/router.py
  - apps/api/app/main.py
  - apps/api/tests/neighborhoods/test_models.py
  - apps/api/tests/neighborhoods/test_spatial.py
autonomous: true
requirements: [REQ-003]
must_haves:
  truths:
    - "Admin de área cria/lista/arquiva bairros do catálogo (CRUD area-scoped); bairro só por NOME (sem polígono) é válido"
    - "Polígono entra por GeoJSON Polygon validado server-side (shapely: tipo/anel/self-intersection/limite de vértices) antes do DB"
    - "Persistência espacial via ST_GeomFromGeoJSON; leitura via ST_AsGeoJSON; ponto-em-polígono via ST_Contains (helper único em spatial.py)"
    - "Remover bairro com entregas ativas é bloqueado (arquiva em vez de excluir)"
    - "Cross-area → 404 (não vaza existência)"
  artifacts:
    - path: "apps/api/app/neighborhoods/spatial.py"
      provides: "Helpers func.ST_* (point_in_polygon, geom_from_geojson) — eixo lat-first"
      contains: "ST_Contains"
    - path: "apps/api/app/neighborhoods/service.py"
      provides: "CRUD de bairro area-scoped + validação de polígono + bloqueio de remoção"
      contains: "validate_polygon_geojson"
    - path: "apps/api/app/neighborhoods/router.py"
      provides: "/v1/neighborhoods (admin_area + area_scope)"
      exports: ["router"]
  key_links:
    - from: "apps/api/app/neighborhoods/service.py"
      to: "neighborhoods_catalog (ST_GeomFromGeoJSON)"
      via: "INSERT espacial via SQL Core"
      pattern: "ST_GeomFromGeoJSON"
    - from: "apps/api/app/neighborhoods/router.py"
      to: "area_scope"
      via: "WHERE area_id = scope (404 cross-area)"
      pattern: "AreaScopeDep|area_scope"
---

<objective>
Catálogo de bairros operável: CRUD area-scoped (D-01/D-02, REQ-003) com **polígono opcional** entrando por GeoJSON validado server-side, persistido como tipo espacial nativo do MySQL via `func.ST_*` (sem GeoAlchemy2 — LOW-1 decidido), e o helper de **ponto-em-polígono** (`ST_Contains`) que a Phase 8 consumirá para elegibilidade.

Purpose: é o coração espacial da fase. Bairro por nome já entrega valor (testável em SQLite); polígono destrava cidades de geografia irregular (testável só com MySQL real).
Output: módulo `app/neighborhoods/` completo + endpoints `/v1/neighborhoods` + spatial.py reusável.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-rea-oper-vel-bairros-config-cobertura-e-tabela-de-frete/RESEARCH.md
@.planning/phases/06-rea-oper-vel-bairros-config-cobertura-e-tabela-de-frete/06-CONTEXT.md

# Padrões a reusar
@apps/api/app/couriers/router.py
@apps/api/app/db/mixins.py
@apps/api/app/areas/service.py

<interfaces>
De apps/api/app/auth/dependencies.py:
```python
AreaScopeDep = Annotated[int | None, Depends(area_scope)]   # area efetiva do token
def require_role(*allowed: str): ...                          # ex.: require_role("admin_area")
CurrentUser = Annotated[User, Depends(get_current_user)]
```
De apps/api/app/db/mixins.py: `BIG_ID`, `UTC_DATETIME`, `AreaScopedMixin`, `TimestampMixin`.
De apps/api/app/core/exceptions.py: `AppError` (status_code/code), `NotFoundError`.

Schema criado pela Plan 01 (migration 0005):
```
neighborhoods_catalog(id, area_id, name, is_informal, polygon POLYGON NULL SRID 4326,
                      archived_at, created_at, updated_at)  -- index (area_id, name)
```

Padrão SQL espacial (RESEARCH Pattern 1/2 — eixo lat-first em WKT SRID 4326):
```python
# INSERT polígono validado:  ST_GeomFromGeoJSON(:gj, 2, 4326)
# POINT à mão de lat/lng Float: ST_GeomFromText('POINT(lat lng)', 4326)  -- lat PRIMEIRO (Pitfall 2)
# ponto-em-polígono:           ST_Contains(polygon, point)
# leitura:                     ST_AsGeoJSON(polygon)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Models + schemas + spatial helpers + validação de polígono</name>
  <files>apps/api/app/neighborhoods/__init__.py, apps/api/app/neighborhoods/models.py, apps/api/app/neighborhoods/schemas.py, apps/api/app/neighborhoods/spatial.py</files>
  <behavior>
    - `validate_polygon_geojson({...})` aceita Polygon válido; rejeita não-Polygon, anel aberto/self-intersection, > max_vertices, coord fora de faixa.
    - `point_in_polygon` monta a query `ST_Contains` com POINT lat-first (sem inverter eixo — Pitfall 2).
    (Validação de polígono é testável em SQLite; ST_Contains é @pytest.mark.mysql.)
  </behavior>
  <skills_applied>domain/mysql-schema-design (POINT/POLYGON SRID 4326, func.ST_*), owasp-security (validação anti-DoS de GeoJSON), domain/fastapi-production-patterns</skills_applied>
  <action>
    1) `models.py`: `class Neighborhood(Base, AreaScopedMixin, TimestampMixin)` mapeando `neighborhoods_catalog` — `id`, `name` String(120), `is_informal` Boolean default False, `archived_at` UTC_DATETIME nullable. **NÃO mapear a coluna `polygon` como tipo ORM** (LOW-1: sem GeoAlchemy2). Polígono é lido/escrito só via SQL Core (`func.ST_*`/`text`). Docstring explica que `polygon` existe no MySQL (DDL 0005) mas é manipulada fora do ORM; em SQLite a coluna não existe → toda asserção espacial é `@pytest.mark.mysql`.
    2) `schemas.py`: `NeighborhoodCreate(name, is_informal=False, polygon_geojson: dict | None)`; `NeighborhoodRead(id, name, is_informal, polygon_status: Literal["defined","by_name"], polygon_geojson: dict | None)`. RFC-7807 para erros.
    3) `spatial.py` (helper ÚNICO — Pitfall 2):
       - `validate_polygon_geojson(gj: dict, *, max_vertices: int = 2000) -> None` usando shapely (Code Example do RESEARCH): tipo == "Polygon", `shape(gj).is_valid`, soma de vértices ≤ max, e checar lat∈[-90,90]/lng∈[-180,180] nas coords. Levanta `InvalidPolygonError(AppError, 422, "invalid_polygon")` com mensagem acionável pt-BR ("GeoJSON inválido. Cole um Polygon com pares lng,lat." / "Coordenada fora de faixa.").
       - `insert_polygon_sql()` / helper que produz o fragmento `ST_GeomFromGeoJSON(:gj, 2, 4326)`.
       - `point_in_polygon(session, *, lat, lng, area_id) -> list[int]`: `select(Neighborhood.id).where(Neighborhood.area_id==area_id, func.ST_Contains(text("polygon"), func.ST_GeomFromText(f"POINT(:lat :lng)",4326)))` — POINT lat-first; usar binds parametrizados (A03, sem f-string de valor).
       - `polygon_as_geojson(session, nbhd_id) -> dict | None` via `ST_AsGeoJSON`.
       Adicionar `shapely` ao projeto: `uv add shapely` (RESEARCH Standard Stack — A5).
  </action>
  <verify>
    <automated>cd apps/api && uv run pytest tests/neighborhoods/test_models.py -x -q && uv run ruff check app/neighborhoods</automated>
  </verify>
  <done>Validação de polígono rejeita malformados/gigantes/fora-de-faixa; helper de ponto-em-polígono usa lat-first e binds parametrizados; bairro por nome persiste/lê em SQLite; shapely instalado; ruff verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Service CRUD area-scoped + bloqueio de remoção + router /v1/neighborhoods</name>
  <files>apps/api/app/neighborhoods/service.py, apps/api/app/neighborhoods/router.py, apps/api/app/main.py</files>
  <behavior>
    - Criar bairro (com/sem polígono); listar bairros da área; arquivar; tentar remover bairro com entregas ativas → bloqueado (mensagem do wireframe).
    - Bairro/área de outro escopo → 404 (não 403).
    (CRUD por nome testável em SQLite; persistência de polígono via ST_GeomFromGeoJSON é @pytest.mark.mysql.)
  </behavior>
  <skills_applied>domain/fastapi-production-patterns, product/api-design-contracts (RFC-7807, contrato /v1), owasp-security (RBAC de área 404), quality/observability-production</skills_applied>
  <action>
    1) `service.py` (area-scoped sempre — `WHERE area_id = scope`):
       - `create_neighborhood(session, *, area_id, body)`: se `polygon_geojson` → `validate_polygon_geojson(...)` então INSERT via SQL Core com `ST_GeomFromGeoJSON(:gj,2,4326)` (Pattern 1); senão INSERT só com nome (polygon NULL). Retornar `Neighborhood` + status de polígono.
       - `list_neighborhoods(session, *, area_id)`: query única (sem N+1), `archived_at IS NULL`, ordenada por nome; status de polígono derivado (defined/by_name) — em MySQL via `polygon IS NOT NULL`; em SQLite sempre by_name.
       - `update_polygon(session, *, area_id, nbhd_id, polygon_geojson)`: valida + `UPDATE ... SET polygon = ST_GeomFromGeoJSON(...)`.
       - `archive_neighborhood(session, *, area_id, nbhd_id)`: set `archived_at` (aware-UTC, TD-010).
       - `remove_neighborhood(...)`: **bloqueio com entregas ativas** — como entregas chegam só na Phase 7, modelar o guard agora: consultar se há dependentes (na ausência da tabela de entregas no M1, o guard verifica a flag de cobertura/uso; documentar que a checagem real de "entregas ativas" será completada na Phase 7). Levantar `NeighborhoodHasActiveDeliveriesError(AppError, 409, "neighborhood_has_active_deliveries")` com a mensagem exata do wireframe: `Não é possível remover "{name}": há entregas ativas nesse bairro. Arquive primeiro.` Recurso inexistente/cross-area → `NotFoundError` (404).
    2) `router.py`: `APIRouter(prefix="/neighborhoods", tags=["neighborhoods"])`, todas as rotas com `require_role("admin_area")` + `AreaScopeDep`; passar `area_id=scope` para o service. Endpoints: `POST ""`, `GET ""`, `PATCH "/{id}/polygon"`, `POST "/{id}/archive"`, `DELETE "/{id}"` (com guard). `await session.commit()` no router (padrão da fase).
    3) `main.py`: registrar `neighborhoods.router` sob `/v1` junto aos demais routers.
  </action>
  <verify>
    <automated>cd apps/api && uv run pytest tests/neighborhoods/test_models.py -x -q && uv run ruff check app/neighborhoods app/main.py</automated>
  </verify>
  <done>CRUD area-scoped funcional; remoção com dependentes bloqueada com a mensagem do wireframe; cross-area → 404; router registrado em /v1; ruff verde.</done>
</task>

</tasks>

## Skills Consultadas
- `domain/mysql-schema-design` — `func.ST_*` nativo (ST_GeomFromGeoJSON/ST_Contains/ST_AsGeoJSON); eixo lat-first em WKT SRID 4326 (Pitfall 2); poligono fora do ORM (sem GeoAlchemy2 — LOW-1); area-scoped.
- `owasp-security` — validacao anti-DoS de GeoJSON server-side (shapely: tipo/anel/self-intersection/<=2000 vertices/faixa lat-lng) ANTES do DB; binds parametrizados (A03, sem f-string de valor); RBAC de area 404 cross-area.
- `domain/fastapi-production-patterns` — modulo `app/neighborhoods/` (models/schemas/spatial/service/router); commit no router (padrao da fase).
- `product/api-design-contracts` — `/v1/neighborhoods` RFC-7807; mensagens de erro estaveis consumidas pela UI (Plan 04).
- `quality/observability-production` — log de CRUD com `request_id`/`area_id`/`nbhd_id`; nunca logar GeoJSON inteiro (so n de vertices).
- `meta/orchestration-decision-tree` — plan paralela a 03 (arquivos sem overlap: neighborhoods vs couriers) na mesma wave.
- `webapp-testing` — `test_models.py` SQLite (bairro por nome) + `test_spatial.py` `@pytest.mark.mysql` (ponto dentro/fora — criterio de aceite ROADMAP).

## Skills Dispensadas (com justificativa)
- Matriz UI completa + `br/ux-copywriting-ptbr` + `br/brazilian-forms` + `ux-advanced/*` — plano backend-only; a UI do catalogo e a Plan 04.
- `domain/safe2pay-escrow-br` / `domain/saas-billing-canonical` — has_payments:false; sem billing.
- `ux-advanced/file-upload-ux` — poligono e texto GeoJSON, nao arquivo.
- `br/lgpd-compliance` — bairros/poligonos nao sao PII (has_pii:false).
- `mobile/*` / `ux-advanced/chat-ux-patterns` — sem mobile UI / chat aqui.

## Performance budget
- `list_neighborhoods`: query unica ordenada por nome, `archived_at IS NULL` (sem N+1); poucos bairros/area no piloto.
- `point_in_polygon`: ST_Contains no MySQL (indexado quando houver SPATIAL INDEX; M1 sem indice — TD-017). Nao recalcular ponto-em-poligono no Python (anti-pattern do RESEARCH).
- Insercao de poligono validada no app antes do DB (anti-DoS de poligono gigante).

## Integration contracts
- N/A (integration_check:false).

## Tech debt deste plano
- Herdada TD-017 (SPATIAL INDEX nullable) — definida na Plan 01.
- `remove_neighborhood`: o guard de "entregas ativas" sera completado na Phase 7 (tabela de entregas ainda nao existe no M1); registrar SUG na execucao se necessario.


<security_validation_notes>
## Security & Validation Notes (subset desta plan — ver Plan 01 para a lista completa dos 6)
1. **GeoJSON anti-DoS** (item 1): `validate_polygon_geojson` server-side ANTES do DB — tipo Polygon, anel fechado, sem self-intersection (`shapely.is_valid`), ≤2000 vértices, lat/lng em faixa. [implementado — Task 1]
2. **RBAC de área** (item 2): todas as rotas `require_role("admin_area")` + `AreaScopeDep`; `WHERE area_id`; cross-area → **404, não 403**. [implementado — Task 2]
> Threat model pesado N/A (has_pii:false / has_payments:false / has_external_integration:false). Integration contracts N/A (integration_check:false).
</security_validation_notes>

<observability_checklist>
## Observability checklist
- `neighborhood.create/update_polygon/archive`: log estruturado com `request_id`, `area_id`, `nbhd_id`; nunca logar o GeoJSON inteiro (pode ser grande — logar só nº de vértices).
- Rejeição de polígono inválido (422): log com motivo + `request_id`.
- Sem audit_log obrigatório para bairro (não é config sensível); usar audit só se o dono pedir trilha de CRUD de catálogo (não requerido pelo RESEARCH).
</observability_checklist>

<error_ux_checklist>
## Error UX checklist (backend — mensagens consumidas pela UI da Plan 04)
- GeoJSON inválido → 422 "GeoJSON inválido. Cole um Polygon com pares lng,lat." / "Coordenada fora de faixa. Use lng entre −180 e 180."
- Remoção bloqueada → 409 `Não é possível remover "{name}": há entregas ativas nesse bairro. Arquive primeiro.`
</error_ux_checklist>

<low_confidence_resolution>
## LOW confidence → task (Regra 12)
- **LOW-1** (GeoAlchemy2 vs nativo): IMPLEMENTADO nativo (`func.ST_*`, sem mapear `polygon` no ORM). Decisão travada.
- **LOW-3** (formato de polígono M1): IMPLEMENTADO — endpoint aceita **GeoJSON Polygon**, valida (shapely), persiste via `ST_GeomFromGeoJSON`. WKT/coordenadas brutas ficam secundárias (não no M1; a UI cola GeoJSON).
- **LOW-2** (testar spatial): `test_spatial.py` @pytest.mark.mysql exercita `point_in_polygon` (1 dentro, 1 fora) — fica verde no CI MySQL.
</low_confidence_resolution>

<verification>
- `cd apps/api && uv run pytest tests/neighborhoods/test_models.py -x` → verde (SQLite).
- `uv run pytest -m mysql tests/neighborhoods/test_spatial.py -x` → verde no CI MySQL (ponto dentro/fora decide elegibilidade — critério de aceite do ROADMAP).
- `uv run ruff check app/neighborhoods` → verde.
</verification>

<success_criteria>
- Catálogo de bairros CRUD area-scoped (REQ-003); bairro por nome válido; polígono opcional via GeoJSON validado.
- `point_in_polygon` (ST_Contains, lat-first) pronto para a Phase 8.
- Remoção com entregas ativas bloqueada; cross-area → 404.
- Teste espacial @pytest.mark.mysql provando elegibilidade por ponto-em-polígono.
</success_criteria>

<output>
After completion, create `.planning/phases/06-rea-oper-vel-bairros-config-cobertura-e-tabela-de-frete/06-02-SUMMARY.md`
</output>
