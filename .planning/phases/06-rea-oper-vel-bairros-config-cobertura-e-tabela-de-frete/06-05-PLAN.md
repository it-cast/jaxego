---
phase: 06-rea-oper-vel-bairros-config-cobertura-e-tabela-de-frete
plan: 05
type: execute
wave: 3
depends_on: [01, 03]
files_modified:
  - apps/web/src/features/entregador/cobertura-precos/cobertura-precos.page.ts
  - apps/web/src/features/entregador/cobertura-precos/cobertura-precos.page.scss
  - apps/web/src/features/entregador/cobertura-precos/coverage-list.component.ts
  - apps/web/src/features/entregador/cobertura-precos/cobertura-precos.stories.ts
  - apps/web/src/features/entregador/cobertura-precos/cobertura-precos.service.ts
  - apps/web/src/features/entregador/disponibilidade/availability-toggle.component.ts
  - apps/web/src/features/entregador/disponibilidade/availability-toggle.stories.ts
autonomous: false
requirements: [REQ-016, REQ-017, REQ-018]
must_haves:
  truths:
    - "Entregador (tela 10, Ionic mobile) seleciona bairros que atende (checkbox) e define preço por bairro OU por km, com validação de PISO que cita o valor ao rejeitar (RN-015)"
    - "Aviso de cobertura (RN-003): só recebe ofertas com coleta E entrega nos seus bairros; piso da cidade vem da config da área (mono, não hardcoded)"
    - "jx-availability-toggle online/offline (role=switch); só active liga; não-active → warn-banner 'termine sua validação'"
    - "Touch ≥44px; AA claro+dark; safe-area; prefers-reduced-motion"
  artifacts:
    - path: "apps/web/src/features/entregador/cobertura-precos/cobertura-precos.page.ts"
      provides: "Tela 10 cobertura + preços + validação de piso"
      min_lines: 40
    - path: "apps/web/src/features/entregador/disponibilidade/availability-toggle.component.ts"
      provides: "jx-availability-toggle online/offline"
      min_lines: 25
  key_links:
    - from: "apps/web/src/features/entregador/cobertura-precos/cobertura-precos.service.ts"
      to: "/v1/couriers/{id}/coverage + /pricing"
      via: "HttpClient PUT"
      pattern: "coverage|pricing"
    - from: "apps/web/src/features/entregador/disponibilidade/availability-toggle.component.ts"
      to: "/v1/couriers/{id}/availability"
      via: "HttpClient PATCH"
      pattern: "availability"
---

<objective>
Superfície **Entregador (Ionic 8, mobile-first)** da Phase 6: tela 10 (**cobertura + preços** com validação de piso — RN-003/RN-015, D-04/D-05) e o **`jx-availability-toggle`** online/offline (D-06), reusando o shell Ionic e o design system Phase 3/5 conforme o UI-SPEC §4–§5.

Purpose: o entregador define onde atende (vale para coleta E entrega) e quanto cobra (livre acima do piso — a plataforma nunca fixa preço, só rejeita abaixo do piso citando o valor), e fica online apenas se `active`.
Output: tela de cobertura/preços + toggle de disponibilidade, com stories/baseline mobile.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-rea-oper-vel-bairros-config-cobertura-e-tabela-de-frete/UI-SPEC.md
@.planning/phases/06-rea-oper-vel-bairros-config-cobertura-e-tabela-de-frete/06-CONTEXT.md
@docs/identidade-visual/tokens.json
@projeto/wireframes/10-entregador-cobertura-precos.html

# Shell Ionic + padrões a reusar (Phase 3/5)
@apps/web/src/features/entregador/cadastro

<interfaces>
Design system herdado (Phase 3): vars semânticas `--surface(-elevated/-sunken)`, `--text(-muted/-subtle)`, `--border`, `--brand(-contrast/-wash)`, `--success(-bg)`, `--warning(-bg)`, `--error(-bg)`, `--info`, `--focus-ring`; `jx-field`, `jx-empty-state`, `jx-error-state` (role=alert), `jx-warn-banner` (role=status), `jx-loading-skeleton`. Shell `ion-tabs`. (UI-SPEC §9 — todas em tokens.json.)

Contratos de API (Plan 03):
- `GET/PUT /v1/couriers/{id}/coverage` body {includes:[nbhd_id], excludes:[nbhd_id]} (RN-003)
- `GET/PUT /v1/couriers/{id}/pricing` body {mode:'neighborhood'|'km', rows:[{neighborhood_id?, up_to_km?, price, return_pct?}]} — 422 "price_below_floor" cita o piso (RN-015)
- `PATCH /v1/couriers/{id}/availability` {online:bool} — 409 "courier_not_active" se não active
- Piso da área (piso_entrega/piso_km) exposto para a UI exibir e validar inline (vem da config da área).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tela 10 — cobertura + preços (modo bairro/km) + validação de piso</name>
  <files>apps/web/src/features/entregador/cobertura-precos/cobertura-precos.page.ts, apps/web/src/features/entregador/cobertura-precos/cobertura-precos.page.scss, apps/web/src/features/entregador/cobertura-precos/coverage-list.component.ts, apps/web/src/features/entregador/cobertura-precos/cobertura-precos.service.ts, apps/web/src/features/entregador/cobertura-precos/cobertura-precos.stories.ts</files>
  <skills_applied>ux-advanced/gesture-touch-patterns (alvos ≥44px, toque na linha, scale .97, sem swipe destrutivo), br/brazilian-forms (máscara monetária R$ 0,00, inputmode decimal/numeric, nunca type=number cru), ux-advanced/form-ux-mastery (validação no blur, CTA habilita só válido), quality/error-ux-patterns (erro de piso cita o piso, role=alert), br/ux-copywriting-ptbr, ui-ux-pro-max (mono em R$/km/%, Fraunces 1 palavra, persimmon única ação), quality/accessibility-pro (role=radiogroup no modo, aria-describedby, AA), ux-advanced/empty-states-polish (sem cobertura ainda), ux-advanced/responsive-breakpoint-strategy (mobile ≤420px), ux-advanced/dark-mode-theming (DEC-001)</skills_applied>
  <action>
    Página Ionic conforme UI-SPEC §4 (wireframe 10). `ion-content` `--surface`, max-width 420px, padding 16, **safe-area insets**. H1 "Bairros e *preços*" (*preços* Fraunces italic `--brand`).
    **Aviso RN-003 (`jx-warn-banner` role=status, não-dispensável):** "Você só recebe ofertas quando a coleta **E** a entrega estão nos seus bairros. Piso da cidade: **R$ X,XX**." — piso vindo da config da área (mono, NUNCA hardcoded).
    **Seletor de modo** (segmented `role="radiogroup"`) "Por bairro" / "Por km"; trocar de modo preserva os dados do outro.
    `coverage-list.component.ts` (modo bairro): lista vertical dos bairros do catálogo; cada linha = checkbox "atendo" (≥44px área de toque) + nome + `jx-field` preço compacto `inputmode="decimal"` máscara `R$ 0,00` mono à direita; bairro não atendido → input disabled (placeholder "—"). Linha inteira tocável (scale .97 `motion.fast`); `prefers-reduced-motion` desliga. Exclusão (RN-003/D-04): estado "Excluir" com selo "Excluído" (texto+ícone) que prevalece sobre cobertura.
    **Modo km:** lista editável de faixas `{até_km (inputmode numeric), preço (inputmode decimal)}` + "Adicionar faixa"; seleção de bairros continua visível (sem input de preço por bairro).
    **Retorno:** `jx-field` `inputmode="numeric"` 0–100 "% sobre a corrida para voltar com o item" (default herdado da política da área).
    **Validação de PISO (RN-015 — núcleo):** no blur do preço, se < piso do modo → `jx-field` em erro (`aria-invalid`, mensagem por `aria-describedby`). Ao salvar, se algum preço < piso → bloqueia + `jx-error-state` `role="alert"` acima do CTA: "O preço de {bairro} está abaixo do piso da cidade (R$ X,XX). Ajuste para salvar." (piso SEMPRE citado, mono; mapeia o 422 do backend). Foco move ao alerta. Sem dark pattern: pode sugerir, nunca força.
    CTA "Salvar bairros e preços" full-width `--brand` ≥44px; salvando = disabled + skeleton + `aria-busy`.
    **Vazio:** `jx-empty-state` "Você ainda não atende nenhum bairro. Marque os bairros que quer atender abaixo." (sem CTA falso).
    `cobertura-precos.service.ts`: HttpClient `GET/PUT /v1/couriers/{id}/coverage` e `/pricing`. Stories: modo-bairro, modo-km, preço-abaixo-do-piso, retorno, sem-cobertura · claro+dark · mobile.
  </action>
  <verify>
    <automated>cd apps/web && npm run test -- --include='**/cobertura-precos*.spec.ts' --watch=false 2>/dev/null || npm run build</automated>
  </verify>
  <done>Tela 10 com seleção de bairros (checkbox ≥44px), modo bairro/km, máscara monetária, retorno, e validação de piso que CITA o piso ao rejeitar (role=alert); estado vazio; touch/AA/safe-area/reduced-motion; zero #hex claro+dark; build/teste verde.</done>
</task>

<task type="auto">
  <name>Task 2: jx-availability-toggle — online/offline (só active)</name>
  <files>apps/web/src/features/entregador/disponibilidade/availability-toggle.component.ts, apps/web/src/features/entregador/disponibilidade/availability-toggle.stories.ts</files>
  <skills_applied>product/component-library-governance (componente governado + baseline), quality/accessibility-pro (role=switch, aria-checked, aria-live, status texto+ícone não só cor), ux-advanced/gesture-touch-patterns (≥44px, feedback de toque), quality/error-ux-patterns (warn-banner não-active), br/ux-copywriting-ptbr, ui-ux-pro-max (sem toggle neon, sem festividade), ux-advanced/dark-mode-theming (DEC-001)</skills_applied>
  <action>
    `jx-availability-toggle` conforme UI-SPEC §5. `role="switch"` + `aria-checked`; pílula `radius.full` ≥44px altura. **Online** = trilho `--success` + texto "Online" + ícone/ponto; **offline** = trilho `--surface-sunken` + "Offline" `--text-muted`. Status por **texto + posição + ícone**, nunca só cor.
    **Só `active` liga (D-06):** se o courier não é `active` (input `disabled=true`), o toggle fica **desabilitado** e renderiza `jx-warn-banner` (role=status): "Termine sua validação para ficar online e receber ofertas." + CTA "Ver validação" (`--info`). Não-bloqueante mas o toggle não liga.
    Ao alternar: `PATCH /v1/couriers/{id}/availability` {online}; transição `motion.normal`; estado anunciado `aria-live="polite"` ("Você está online"/"Você está offline"). `prefers-reduced-motion` desliga transição. Tratar 409 do backend (não-active) revertendo o estado + exibindo o warn-banner.
    Stories: online, offline, desabilitado (não-active) · claro+dark · mobile.
  </action>
  <verify>
    <automated>cd apps/web && npm run test -- --include='**/availability-toggle*.spec.ts' --watch=false 2>/dev/null || npm run build</automated>
  </verify>
  <done>Toggle role=switch online/offline; status texto+ícone; não-active → disabled + warn-banner com CTA; aria-live anuncia troca; trata 409; reduced-motion; zero #hex claro+dark; build/teste verde.</done>
</task>

</tasks>

## Skills Consultadas
- `ui-ux-pro-max` — mono em R$/km/%; Fraunces italic em 1 palavra do H1 (precos); persimmon unica acao; anti AI-slop (sem toggle neon, sem festividade ao salvar); tela do entregador limpa e direta.
- `quality/accessibility-pro` — `role="switch"`/`aria-checked` no toggle; `role="radiogroup"` no modo bairro/km; `aria-live="polite"` anuncia online/offline; status texto+icone (nunca so cor); AA claro+dark; foco visivel.
- `ux-advanced/gesture-touch-patterns` — alvos >=44px (checkbox atendo, input preco, segmented, toggle, CTA); linha tocavel; feedback scale .97 `motion.fast`; sem swipe destrutivo; `prefers-reduced-motion`.
- `ux-advanced/design-tokens-system` — so vars semanticas; nenhum #hex; nenhuma var nova.
- `product/component-library-governance` — `jx-availability-toggle` e `coverage-list` governados (story + baseline mobile).
- `br/brazilian-forms` — mascara monetaria pt-BR `R$ 0,00` (inputmode decimal), inputmode numeric para km/%, NUNCA type=number cru para dinheiro.
- `ux-advanced/form-ux-mastery` — validacao no blur do preco; um erro por campo via `aria-describedby`; CTA habilita so com form valido.
- `quality/error-ux-patterns` — `jx-error-state` role=alert "preco abaixo do piso (R$ X,XX)" CITANDO o piso (RN-015); `jx-warn-banner` role=status "termine sua validacao para ficar online".
- `br/ux-copywriting-ptbr` — sentence case; CTA verbo+objeto; erro acionavel.
- `ux-advanced/empty-states-polish` — `jx-empty-state` "voce ainda nao atende nenhum bairro" (sem CTA falso — a acao sao os checkboxes).
- `ux-advanced/responsive-breakpoint-strategy` — mobile-first, <=420px (wireframe 10), safe-area insets.
- `ux-advanced/dark-mode-theming` (DEC-001) — AA nos dois temas; trilho do toggle e badges no dark via cor viva sobre superficie escura.
- `product/visual-regression-testing` — stories claro+dark+mobile (modo-bairro, modo-km, preco-abaixo-do-piso, sem-cobertura, toggle online/offline/desabilitado).
- `ux-advanced/saas-dashboard-patterns` — citada de leve: feedback discreto ao salvar (sem toast festivo), coerente com o admin.

## Skills Dispensadas (com justificativa)
- `ux-advanced/data-tables-ux` — a tela do entregador e lista vertical mobile (coverage-list), nao tabela densa; `jx-data-table` e da Plan 04 (admin).
- `ux-advanced/file-upload-ux` — sem upload nesta tela.
- `ux-advanced/onboarding-patterns` / `ux-advanced/trust-safety-ux` — courier ja cadastrado (Phase 5); o toggle so reflete o status KYC, nao refaz onboarding.
- `domain/safe2pay-escrow-br` / `domain/saas-billing-canonical` / `ux-advanced/payment-checkout-ux` — has_payments:false; o entregador define preco (guard-rail de piso), nao ha cobranca/checkout aqui (Phase 10/11).
- `ux-advanced/chat-ux-patterns` / `mobile/push-notifications-architecture` — sem chat/push nesta fase (push e Phase 8/9).
- `br/lgpd-compliance` — has_pii:false; nenhum dado pessoal novo exibido alem do permitido.

## Performance budget
- LCP < 2500ms / INP < 200ms (mobile 4G; config.json). Sem mapa (Phase 9). Rotas lazy. Lista de cobertura sem virtualizacao (poucos bairros/area no piloto).
- Feedback de toque <=140ms (`motion.fast`); transicoes respeitam `prefers-reduced-motion`.

## Integration contracts
- N/A (integration_check:false). Consome `/v1/couriers/{id}/coverage|pricing|availability` (Plan 03) — contrato por tipos de resposta; 422 (piso) e 409 (nao-active) tratados na UI.

## Tech debt deste plano
- Nenhuma TD nova obrigatoria. O consumo do estado online pelo despacho (cascata) e Phase 8 — aqui so o toggle e o estado pronto.


<security_validation_notes>
## Security & Validation Notes (frontend — backend é a autoridade)
2. **RBAC:** a UI chama só os endpoints do PRÓPRIO courier (self-only, area-scoped); cross retorna 404 (Plan 03) — a UI não expõe dados de outro entregador/área.
4. **Ranges:** validação de `return_pct` 0–100 e preço ≥ piso é feedback de UX; a autoridade é o backend (Plan 03).
6. **Online só active:** o guard real é server-side (409); a UI desabilita o toggle e explica — mas confia no backend.
> has_pii:false. Threat model pesado N/A. Integration contracts N/A (integration_check:false).
</security_validation_notes>

<observability_checklist>
## Observability checklist (frontend)
- 422 (piso) e 409 (não-active) capturados e exibidos com copy acionável; `request_id` não exibido.
- O piso exibido vem sempre da config da área (fonte única) — nunca constante no front.
</observability_checklist>

<error_ux_checklist>
## Error UX checklist (UI)
- Preço abaixo do piso → `jx-error-state` role=alert "O preço de {bairro} está abaixo do piso da cidade (R$ X,XX). Ajuste para salvar." (piso citado, mono).
- Online sem ser active → `jx-warn-banner` role=status "Termine sua validação para ficar online e receber ofertas." + "Ver validação"; toggle não liga.
- Sem cobertura → `jx-empty-state` (status, não alerta).
</error_ux_checklist>

<verification>
- `cd apps/web && npm run build` → verde.
- `grep -r "#[0-9A-Fa-f]\{6\}" apps/web/src/features/entregador/cobertura-precos apps/web/src/features/entregador/disponibilidade --include=*.scss | grep -v tokens` → 0 (Gate 2).
- axe sem violações críticas na tela 10 (verificação ROADMAP), claro+dark, mobile.
- Wireframe-contract de 10 coberto.
</verification>

<success_criteria>
- Tela 10: cobertura (RN-003, REQ-016) + tabela de frete por bairro/km com validação de piso citando o piso (RN-015, REQ-017).
- jx-availability-toggle online/offline só para active (REQ-018, D-06).
- Gate 2: zero #hex; AA claro+dark (DEC-001); touch ≥44px; safe-area; reduced-motion.
</success_criteria>

<output>
After completion, create `.planning/phases/06-rea-oper-vel-bairros-config-cobertura-e-tabela-de-frete/06-05-SUMMARY.md`
</output>
