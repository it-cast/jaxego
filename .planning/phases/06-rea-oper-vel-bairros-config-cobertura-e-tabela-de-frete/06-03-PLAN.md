---
phase: 06-rea-oper-vel-bairros-config-cobertura-e-tabela-de-frete
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - apps/api/app/couriers/models.py
  - apps/api/app/couriers/coverage.py
  - apps/api/app/couriers/pricing.py
  - apps/api/app/couriers/availability.py
  - apps/api/app/couriers/schemas.py
  - apps/api/app/couriers/router.py
  - apps/api/tests/couriers/test_coverage.py
  - apps/api/tests/couriers/test_pricing_floor.py
  - apps/api/tests/couriers/test_availability.py
autonomous: true
requirements: [REQ-016, REQ-017, REQ-018]
must_haves:
  truths:
    - "Entregador define cobertura (bairros include/exclude); elegibilidade exige cobertura nos DOIS pontos (coleta E entrega); exclusão veta os dois (RN-003)"
    - "Entregador define tabela de frete por bairro OU por km; preço < piso da área → 422 com mensagem que CITA o piso (RN-015); plataforma nunca fixa o preço"
    - "Entregador alterna online/offline; só courier active fica online; busy é derivado (não persistido editável)"
    - "Todas as rotas area-scoped; cross-area → 404"
  artifacts:
    - path: "apps/api/app/couriers/pricing.py"
      provides: "Validação de piso (RN-015) — rejeição citando o piso por modo"
      contains: "PriceBelowFloorError"
    - path: "apps/api/app/couriers/coverage.py"
      provides: "Cobertura include/exclude + regra de elegibilidade nos dois pontos"
      contains: "is_eligible"
    - path: "apps/api/app/couriers/availability.py"
      provides: "online/offline (só active); busy derivado da carga"
      contains: "max_concurrent"
  key_links:
    - from: "apps/api/app/couriers/pricing.py"
      to: "Area.config (piso_km / piso_entrega)"
      via: "leitura do piso da área para validar"
      pattern: "piso_km|piso_entrega"
    - from: "apps/api/app/couriers/availability.py"
      to: "Courier.status == active"
      via: "guard de online só para active"
      pattern: "active"
---

<objective>
Lado da oferta pronto para o despacho (Phase 8): **cobertura** do entregador nos dois pontos (RN-003, REQ-016), **tabela de frete** com guard-rail de piso (RN-015, REQ-017) e **disponibilidade** online/offline (REQ-018), tudo area-scoped sobre a entidade `couriers` da Phase 5.

Purpose: a plataforma NUNCA fixa o preço — só impõe piso e rejeita abaixo dele citando o valor. Cobertura nos dois pontos evita recusa pós-aceite. Estado online fica pronto sem o consumo de despacho (Phase 8).
Output: `coverage.py`, `pricing.py`, `availability.py` + extensão de `Courier` + endpoints.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-rea-oper-vel-bairros-config-cobertura-e-tabela-de-frete/RESEARCH.md
@.planning/phases/06-rea-oper-vel-bairros-config-cobertura-e-tabela-de-frete/06-CONTEXT.md
@projeto/regras-negocio/regras.md

# Entidade e padrões a reusar (Phase 5)
@apps/api/app/couriers/models.py
@apps/api/app/couriers/router.py
@apps/api/app/couriers/service.py

<interfaces>
De apps/api/app/couriers/models.py:
```python
COURIER_STATUSES = ("pending_kyc", "active", "suspended", "banned")
class Courier(Base, AreaScopedMixin, TimestampMixin):
    id; area_id; user_id; cpf; status; ...
    # Plan 01 adicionou: is_online (Boolean), max_concurrent (Integer)
```
De apps/api/app/auth/dependencies.py: `require_role("courier"|"admin_area")`, `AreaScopeDep`, `CurrentUser`.
De apps/api/app/core/exceptions.py: `AppError(status_code, code)`, `NotFoundError`.

Schema criado pela Plan 01 (migration 0005):
```
courier_coverage_areas(id, area_id, courier_id, neighborhood_id, kind['include'|'exclude'], ts)  UNIQUE(courier_id,neighborhood_id)
courier_pricing_tables(id, area_id, courier_id, mode['neighborhood'|'km'], neighborhood_id?, up_to_km?, price Numeric(10,2), return_pct?, ts)
couriers.is_online (Boolean), couriers.max_concurrent (Integer)
```

Validação de piso (RESEARCH Code Example — mensagem CITA o piso):
```python
class PriceBelowFloorError(AppError):
    status_code = 422; code = "price_below_floor"
    # "Preço abaixo do piso da área (por km|por entrega): mínimo R$ X,XX."
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cobertura (RN-003) + validação de piso (RN-015)</name>
  <files>apps/api/app/couriers/coverage.py, apps/api/app/couriers/pricing.py, apps/api/app/couriers/schemas.py, apps/api/tests/couriers/test_coverage.py, apps/api/tests/couriers/test_pricing_floor.py</files>
  <behavior>
    - is_eligible: pickup E dropoff ambos em include e nenhum em exclude → True; faltando um, ou um excluído → False.
    - assert_above_floor: mode 'neighborhood' valida piso_entrega; mode 'km' valida piso_km; preço < piso → PriceBelowFloorError com piso na mensagem.
  </behavior>
  <skills_applied>domain/fastapi-production-patterns, product/api-design-contracts (RFC-7807 422), quality/error-ux-patterns (mensagem cita o piso), owasp-security (validação de input / ranges return_pct 0-100)</skills_applied>
  <action>
    1) `coverage.py`:
       - `set_coverage(session, *, area_id, courier_id, includes: list[int], excludes: list[int])`: substitui as linhas de cobertura do courier (delete + insert area-scoped); valida que cada `neighborhood_id` pertence à mesma área (senão 404/422). UNIQUE(courier_id, neighborhood_id) garante 1 linha por bairro; um bairro não pode estar em include E exclude (rejeitar).
       - `list_coverage(session, *, area_id, courier_id)`: query única.
       - `is_eligible(coverage_rows, pickup_nbhd_id, dropoff_nbhd_id) -> bool`: implementar Pattern 3 do RESEARCH (exclude veta os dois; ambos precisam estar em include). Função PURA (testável em SQLite, base da Phase 8).
    2) `pricing.py`:
       - `PriceBelowFloorError(AppError)` (422, "price_below_floor") cuja msg CITA o piso por unidade — usar o Code Example do RESEARCH: "Preço abaixo do piso da área (por km): mínimo R$ X,XX." (`{floor:.2f}`).
       - `assert_above_floor(price, *, floor_km, floor_entrega, mode)`: mode 'km' < floor_km → erro km; mode 'neighborhood' < floor_entrega → erro entrega.
       - `set_pricing(session, *, area_id, courier_id, mode, rows)`: lê o piso da `Area.config` (piso_km/piso_entrega via AreaConfig da Plan 01); para cada linha (`price`, opcional `neighborhood_id`/`up_to_km`, `return_pct` 0–100) chama `assert_above_floor`; persiste area-scoped. **A plataforma nunca fixa preço** — só rejeita abaixo do piso (pode calcular *sugestão* não obrigatória).
       - **LOW-5 (aumento de piso retroativo):** NÃO bloquear retroativamente linhas já salvas quando o piso da área subir; apenas o submit novo valida. Registrar TD (ver low_confidence_resolution).
    3) `schemas.py`: `CoverageBody(includes: list[int], excludes: list[int])`; `PricingBody(mode: Literal["neighborhood","km"], rows: list[PricingRow])`; `PricingRow(neighborhood_id?, up_to_km?, price: Decimal ge=0, return_pct: int|None ge=0 le=100)`. RFC-7807.
    4) Tests (SQLite): completar `test_coverage.py` (dois pontos + exclusão) e `test_pricing_floor.py` (piso km e entrega; mensagem cita o piso).
  </action>
  <verify>
    <automated>cd apps/api && uv run pytest tests/couriers/test_coverage.py tests/couriers/test_pricing_floor.py -x -q && uv run ruff check app/couriers/coverage.py app/couriers/pricing.py</automated>
  </verify>
  <done>Cobertura exige os DOIS pontos e exclusão veta ambos (RN-003); preço < piso → 422 com piso citado por modo (RN-015); return_pct em 0–100; cross-area validado; ruff verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Disponibilidade online/offline (só active) + router /v1/couriers</name>
  <files>apps/api/app/couriers/availability.py, apps/api/app/couriers/models.py, apps/api/app/couriers/router.py, apps/api/tests/couriers/test_availability.py</files>
  <behavior>
    - set_online(courier active) → is_online=True; courier pending_kyc/suspended/banned → 409 (não pode ficar online).
    - busy é DERIVADO: helper compute_busy(active_count, max_concurrent) sem persistir campo busy.
  </behavior>
  <skills_applied>domain/fastapi-production-patterns, owasp-security (não vazar disponibilidade de quem não pode operar — item 6), product/api-design-contracts, quality/observability-production</skills_applied>
  <action>
    1) `models.py`: confirmar os atributos `is_online: Mapped[bool]` (default False) e `max_concurrent: Mapped[int]` (default 1) no `Courier` (colunas criadas na migration 0005 da Plan 01). NÃO adicionar coluna `busy`.
    2) `availability.py`:
       - `set_availability(session, *, area_id, courier_id, online: bool)`: carrega courier area-scoped (404 cross-area); se `online=True` e `courier.status != "active"` → `CannotGoOnlineError(AppError, 409, "courier_not_active")` com msg pt-BR ("Termine sua validação para ficar online e receber ofertas." — alinha com o warn-banner da Plan 05). Senão set `is_online`.
       - `compute_busy(active_deliveries: int, max_concurrent: int) -> bool`: `active_deliveries >= max_concurrent`. Função PURA, derivada — sem persistência (a contagem real de entregas ativas vem da Phase 8/7; aqui só o helper + max_concurrent persistido). Documentar.
       - `set_max_concurrent(...)`: valida `>= 1` (Security item 4).
    3) `router.py` (estender `/v1/couriers`): adicionar rotas com `require_role("courier")` (o próprio entregador) + `AreaScopeDep`:
       - `PUT "/{id}/coverage"` → coverage.set_coverage
       - `PUT "/{id}/pricing"` → pricing.set_pricing
       - `PATCH "/{id}/availability"` → availability.set_availability
       - `GET "/{id}/coverage"`, `GET "/{id}/pricing"`
       Garantir que o courier só edita o PRÓPRIO registro (id do token == id da rota, area-scoped) — cross-courier/cross-area → 404. `await session.commit()`.
    4) `test_availability.py` (SQLite): só active fica online; não-active → 409; `compute_busy` derivado.
  </action>
  <verify>
    <automated>cd apps/api && uv run pytest tests/couriers/test_availability.py -x -q && uv run ruff check app/couriers</automated>
  </verify>
  <done>Online só para active (409 caso contrário); busy derivado via compute_busy (sem coluna); max_concurrent ≥ 1; rotas area-scoped e self-only (404 caso contrário); ruff verde.</done>
</task>

</tasks>

## Skills Consultadas
- `domain/fastapi-production-patterns` — modulos `coverage.py`/`pricing.py`/`availability.py`; funcoes puras (is_eligible, assert_above_floor, compute_busy) testaveis em SQLite; router self-only + area-scoped.
- `quality/error-ux-patterns` — `PriceBelowFloorError` (422) cuja mensagem CITA o piso por modo (RN-015); `CannotGoOnlineError` (409) com copy alinhada ao warn-banner da Plan 05.
- `product/api-design-contracts` — `/v1/couriers/{id}/coverage|pricing|availability` RFC-7807; contratos consumidos pela Plan 05.
- `owasp-security` — RBAC: courier edita so o proprio registro, area-scoped, cross -> 404 (item 2/6); ranges (return_pct 0-100, price>=0, max_concurrent>=1, item 4); online so para active (item 6 — nao vaza disponibilidade de quem nao pode operar).
- `quality/observability-production` — log de coverage/pricing/availability com `request_id`/`area_id`/`courier_id`; PII do courier nunca logada (TH-05 herdado da Phase 5).
- `meta/orchestration-decision-tree` — plan paralela a 02 (couriers vs neighborhoods sem overlap de arquivos).
- `webapp-testing` — `test_coverage.py` (dois pontos + exclusao), `test_pricing_floor.py` (piso km/entrega cita piso), `test_availability.py` (so active online; busy derivado) — todos SQLite.

## Skills Dispensadas (com justificativa)
- Matriz UI completa + `br/ux-copywriting-ptbr` + `br/brazilian-forms` + `ux-advanced/*` — plano backend-only; a UI (tela 10) e a Plan 05.
- `domain/mysql-schema-design` — schema ja criado na Plan 01 (migration 0005); aqui so consome as colunas; sem DDL novo.
- `domain/safe2pay-escrow-br` / `domain/saas-billing-canonical` — has_payments:false; a tabela de frete e guard-rail de PISO, NAO cobranca/split (a plataforma nunca fixa preco — RN-015). Billing real e Phase 10.
- `br/lgpd-compliance` — cobertura/preco/disponibilidade nao introduzem PII nova.
- `mobile/*` / `ux-advanced/chat-ux-patterns` — sem UI mobile/chat aqui.

## Performance budget
- `set_coverage`/`set_pricing`: delete+insert em lote area-scoped (sem N+1); poucas linhas por courier.
- `is_eligible`/`compute_busy`: funcoes puras O(n) sobre poucas linhas.
- `set_availability`: 1 SELECT (carrega courier) + 1 UPDATE.

## Integration contracts
- N/A (integration_check:false). O estado online/cobertura/preco e CONSUMIDO pela Phase 8 (despacho) — aqui so persistido/pronto.

## Tech debt deste plano
- TD-018 (novo, post_launch_quarter): aumento de piso retroativo (LOW-5) — sinalizar, nao bloquear linhas ja salvas; confirmar UX de aviso com o dono.


<security_validation_notes>
## Security & Validation Notes (subset desta plan — lista completa dos 6 na Plan 01)
2. **RBAC de área** (item 2): rotas `require_role("courier")` + `AreaScopeDep`; courier edita só o próprio registro; cross-area/cross-courier → **404, não 403**. [Task 2]
4. **Ranges numéricos** (item 4): `return_pct` 0–100, `price` ≥ 0, `max_concurrent` ≥ 1 via Pydantic. [Tasks 1/2]
6. **Online só para `active`** (item 6): guard no endpoint de availability — não vaza disponibilidade de quem não pode operar. [Task 2]
> Threat model pesado N/A. Integration contracts N/A (integration_check:false).
</security_validation_notes>

<observability_checklist>
## Observability checklist
- `courier.coverage.update` / `courier.pricing.update` / `courier.availability.update`: log estruturado com `request_id`, `area_id`, `courier_id`, `mode`/`online`; nunca logar PII do courier (TH-05 herdado da Phase 5).
- Rejeição de piso (422): log com `request_id`, modo e piso aplicado (piso não é segredo).
- Tentativa de online por não-active (409): log com `request_id` + status atual.
- Audit_log: cobertura/preço/disponibilidade NÃO são "config sensível da área" (são do entregador sobre si) → não exigem audit before/after pelo RESEARCH; usar audit só se o dono pedir.
</observability_checklist>

<error_ux_checklist>
## Error UX checklist (backend — consumido pela UI da Plan 05)
- Preço abaixo do piso → 422 "Preço abaixo do piso da área (por km|por entrega): mínimo R$ X,XX." (piso citado — RN-015).
- Online sem ser active → 409 "Termine sua validação para ficar online e receber ofertas."
- Bairro de outra área na cobertura → 404/422 (não vaza existência).
</error_ux_checklist>

<low_confidence_resolution>
## LOW confidence → TD (Regra 12)
- **LOW-5** (aumento de piso retroativo): DECISÃO — **sinalizar, não bloquear** linhas já salvas quando o piso subir; submit novo valida. Registrar **TD-018 `post_launch_quarter`**: "Tabelas de frete salvas abaixo de um piso aumentado depois: só sinalizar (não bloquear retroativo). Avaliar UX de aviso ao entregador quando o piso da área subir." (confirmar com dono no discuss da Phase futura).
</low_confidence_resolution>

<verification>
- `cd apps/api && uv run pytest tests/couriers/test_coverage.py tests/couriers/test_pricing_floor.py tests/couriers/test_availability.py -x` → verde.
- `uv run ruff check app/couriers` → verde.
- Critério ROADMAP: preço abaixo do piso → rejeição citando o piso (test_pricing_floor); cobertura vale coleta E entrega (test_coverage).
</verification>

<success_criteria>
- Cobertura nos dois pontos + exclusões (RN-003, REQ-016).
- Tabela de frete com piso citado na rejeição (RN-015, REQ-017); plataforma nunca fixa preço.
- online/offline só para active; busy derivado (REQ-018).
- TD-018 (piso retroativo) registrada.
</success_criteria>

<output>
After completion, create `.planning/phases/06-rea-oper-vel-bairros-config-cobertura-e-tabela-de-frete/06-03-SUMMARY.md`
</output>
