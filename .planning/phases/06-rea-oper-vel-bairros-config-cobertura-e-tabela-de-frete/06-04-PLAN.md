---
phase: 06-rea-oper-vel-bairros-config-cobertura-e-tabela-de-frete
plan: 04
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - apps/web/src/shared/components/data-table/data-table.component.ts
  - apps/web/src/shared/components/data-table/data-table.component.scss
  - apps/web/src/shared/components/data-table/data-table.stories.ts
  - apps/web/src/features/admin/area-config/area-config.page.ts
  - apps/web/src/features/admin/area-config/area-config.page.scss
  - apps/web/src/features/admin/area-config/area-config.stories.ts
  - apps/web/src/features/admin/area-config/area-config.service.ts
  - apps/web/src/features/admin/neighborhoods/neighborhoods.page.ts
  - apps/web/src/features/admin/neighborhoods/neighborhood-row.component.ts
  - apps/web/src/features/admin/neighborhoods/neighborhoods.stories.ts
  - apps/web/src/features/admin/neighborhoods/neighborhoods.service.ts
autonomous: false
requirements: [REQ-002, REQ-003]
must_haves:
  truths:
    - "Admin configura a área (tela 21A): nível KYC, pisos R$ com máscara, geofence (m), timeouts (s), retorno (%) — salvar com confirmação de mudança sensível (before→after)"
    - "jx-data-table é um primitivo compartilhado (header sticky, aria-sort, zebra, estados loading/empty/error embutidos)"
    - "Catálogo de bairros (tela 21B): listar/adicionar/remover (CRUD), polígono opcional por GeoJSON em textarea mono; remoção bloqueada cita o bairro"
    - "Tudo consome só vars semânticas (Gate 2 — nenhum #hex); AA claro+dark (DEC-001)"
  artifacts:
    - path: "apps/web/src/shared/components/data-table/data-table.component.ts"
      provides: "Primitivo jx-data-table acessível"
      min_lines: 40
    - path: "apps/web/src/features/admin/area-config/area-config.page.ts"
      provides: "Form de config da área com máscara monetária e confirmação sensível"
      min_lines: 40
    - path: "apps/web/src/features/admin/neighborhoods/neighborhoods.page.ts"
      provides: "Catálogo de bairros CRUD sobre jx-data-table"
      min_lines: 30
  key_links:
    - from: "apps/web/src/features/admin/area-config/area-config.service.ts"
      to: "/v1/areas/{id} (PATCH config)"
      via: "HttpClient"
      pattern: "areas/.*config|PATCH"
    - from: "apps/web/src/features/admin/neighborhoods/neighborhoods.service.ts"
      to: "/v1/neighborhoods"
      via: "HttpClient CRUD"
      pattern: "neighborhoods"
---

<objective>
Superfície **Admin de área (web desktop-first)** da Phase 6: o formulário de **config da área** (tela 21A — D-03), o primitivo de tabela **`jx-data-table`** (governança — generaliza a fila KYC da Phase 5) e o **catálogo de bairros** (tela 21B — D-01/D-02), reusando integralmente o design system Phase 3/4/5 conforme o UI-SPEC.

Purpose: o admin cura bairros e configura as regras locais (pisos, geofence, timeouts, retorno) com confirmação auditada de mudança sensível. Polígono entra por GeoJSON em texto (sem mapa — Phase 9).
Output: `jx-data-table` + telas de config e catálogo, com stories/baseline.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-rea-oper-vel-bairros-config-cobertura-e-tabela-de-frete/UI-SPEC.md
@.planning/phases/06-rea-oper-vel-bairros-config-cobertura-e-tabela-de-frete/06-CONTEXT.md
@docs/identidade-visual/tokens.json
@projeto/wireframes/21-admin-area-config.html
@projeto/wireframes/17-admin-area-dashboard.html
@projeto/wireframes/18-admin-area-entregadores.html

# Componentes/padrões a REUSAR (não reinventar)
@apps/web/src/features/admin/kyc/queue-table.component.ts
@apps/web/src/features/admin/kyc/review-row.component.ts

<interfaces>
Design system herdado (Phase 3 — consumir, não redefinir):
- Vars semânticas: `--surface`, `--surface-elevated`, `--surface-sunken`, `--text`, `--text-muted`, `--text-subtle`, `--border`, `--brand`, `--brand-contrast`, `--brand-wash`, `--success(-bg)`, `--warning(-bg)`, `--error(-bg)`, `--info(-bg)`, `--focus-ring` (UI-SPEC §9 — todas existem em tokens.json).
- Componentes de estado: `jx-empty-state` (CTA), `jx-error-state` (`role="alert"`), `jx-warn-banner` (`role="status"`), `jx-loading-skeleton`.
- `jx-field` (label + erro + máscara + aria-describedby) — `shared/components/field/field.component.ts`.
- Padrão de fila densa Phase 5: `features/admin/kyc/queue-table.component.ts` (a generalizar em jx-data-table).

Contratos de API (Plans 01/02):
- `PATCH /v1/areas/{id}` body.config = AreaConfig {kyc_level, piso_entrega, piso_km, geofence_m, timeout_oferta_s, timeout_favoritos_s, politica_retorno_pct} — 422 com chave fora de range.
- `POST /v1/neighborhoods` {name, is_informal, polygon_geojson?}; `GET`; `PATCH /{id}/polygon`; `POST /{id}/archive`; `DELETE /{id}` (409 "Não é possível remover ...").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: jx-data-table — primitivo de tabela compartilhado (governança)</name>
  <files>apps/web/src/shared/components/data-table/data-table.component.ts, apps/web/src/shared/components/data-table/data-table.component.scss, apps/web/src/shared/components/data-table/data-table.stories.ts</files>
  <skills_applied>product/component-library-governance (primitivo governado + story/baseline), ux-advanced/data-tables-ux (header sticky, aria-sort, zebra, densidade, estados), quality/accessibility-pro (th scope, caption, teclado), ux-advanced/design-tokens-system (só vars semânticas), product/visual-regression-testing (baseline)</skills_applied>
  <action>
    Criar `jx-data-table` (standalone Angular) conforme UI-SPEC §3.1. `<table>` semântica em container `--surface-elevated`/`--border`/radius lg, `<caption>` (visível ou sr-only). `<thead>` sticky `--surface-sunken`, `<th scope="col">` overline; coluna ordenável com **botão real** + `aria-sort` (ascending/descending/none). Linhas `<td>` `font.size.sm`, divisor `--border`, zebra opcional, hover `--brand-wash`, dados em `font.family.mono`. Ações por linha à direita, área de clique ≥44px; destrutivo em `--error` outline. **Estados embutidos** via input `state: 'loading'|'empty'|'error'|'ready'` → `jx-loading-skeleton` / `jx-empty-state` / `jx-error-state` no lugar do `<tbody>`. Slots de célula (content projection) para reuso. Dark: fundos `_bg` claros viram `--surface-elevated` + cor viva (padrão Phase 3). Nenhum `#hex`; nenhuma var nova.
    `data-table.stories.ts`: ready, com-zebra, ordenado, loading, vazio, erro · claro+dark (UI-SPEC §10).
    Refator da fila KYC (Phase 5) sobre o primitivo é DESEJÁVEL mas NÃO obrigatório nesta task (não redesenhar colunas) — se couber no orçamento, migrar `queue-table` para consumir `jx-data-table`; senão, registrar como SUG para a próxima passada.
  </action>
  <verify>
    <automated>cd apps/web && npm run test -- --include='**/data-table*.spec.ts' --watch=false 2>/dev/null || npm run build</automated>
  </verify>
  <done>jx-data-table renderiza header sticky + aria-sort + 4 estados; stories claro+dark; axe sem violações críticas na story ready; zero #hex (só vars semânticas); build/teste verde.</done>
</task>

<task type="auto">
  <name>Task 2: Tela 21A — Config da área (form + máscara + confirmação sensível)</name>
  <files>apps/web/src/features/admin/area-config/area-config.page.ts, apps/web/src/features/admin/area-config/area-config.page.scss, apps/web/src/features/admin/area-config/area-config.service.ts, apps/web/src/features/admin/area-config/area-config.stories.ts</files>
  <skills_applied>ux-advanced/saas-dashboard-patterns (fieldsets, confirmação de ação sensível), ux-advanced/form-ux-mastery (validação no blur, CTA habilita só com form válido), br/brazilian-forms (máscara monetária pt-BR, inputmode), quality/error-ux-patterns (erro de faixa acionável), br/ux-copywriting-ptbr (sentence case, CTA verbo+objeto), ui-ux-pro-max (mono nos valores, Fraunces 1 palavra, persimmon única ação), quality/accessibility-pro (foco, aria-describedby), ux-advanced/responsive-breakpoint-strategy (desktop-first, 2 col em fieldsets largos), ux-advanced/dark-mode-theming (DEC-001)</skills_applied>
  <action>
    Página de config conforme UI-SPEC §2 (wireframe 21A). Rota sob a sidebar admin `/admin/{area}/config`. `<main>` max-width ~680px; H1 "Configurações da *área* · {Nome}" (*área* em Fraunces italic `--brand`). Form único com 4 `fieldset`s (`<legend>` overline `--text-muted`): **Validação de entregadores** (select Simples/Completa + checkbox antecedentes), **Preços e geofence** (piso_entrega R$, piso_km R$/km com **máscara monetária pt-BR `R$ 0,00`** via `inputmode="decimal"` — NUNCA `type="number"` cru; geofence 30–300 m `inputmode="numeric"`; retorno 0–100 %), **Despacho** (timeout_oferta 10–60 s, timeout_favoritos 30–180 s). Valores em mono alinhados à direita.
    Validação no blur, um erro por campo via `aria-describedby` + `aria-invalid`; faixa fora → mensagem acionável (ex.: "O raio precisa estar entre 30 e 300 metros."). CTA "Salvar configurações (auditado)" `--brand` ≥44px, habilita só com form válido.
    **Confirmação de mudança sensível (saas-dashboard):** ao submeter, diálogo/sheet listando os campos sensíveis alterados em before→after (mono) + copy "Você está alterando regras da área. Isso fica registrado no histórico (audit). Confirmar?". Só após confirmar → `PATCH /v1/areas/{id}` (config). Estado salvando = CTA disabled + skeleton + `aria-busy`; sucesso = inline `--success` `role="status"`; falha = `jx-error-state` `role="alert"` ("Não conseguimos salvar as configurações. Tente de novo em instantes." + retry). Mapear 422 de range do backend ao campo correto.
    `area-config.service.ts`: HttpClient para `GET`/`PATCH /v1/areas/{id}`. Stories: form-completo, validando, confirmação-sensível, erro-faixa · claro+dark.
  </action>
  <verify>
    <automated>cd apps/web && npm run test -- --include='**/area-config*.spec.ts' --watch=false 2>/dev/null || npm run build</automated>
  </verify>
  <done>Form com 4 fieldsets, máscara monetária pt-BR, ranges validados no blur com erro acionável; confirmação before→after antes de salvar; estados salvando/sucesso/erro; zero #hex; AA claro+dark; build/teste verde.</done>
</task>

<task type="auto">
  <name>Task 3: Tela 21B — Catálogo de bairros (CRUD sobre jx-data-table)</name>
  <files>apps/web/src/features/admin/neighborhoods/neighborhoods.page.ts, apps/web/src/features/admin/neighborhoods/neighborhood-row.component.ts, apps/web/src/features/admin/neighborhoods/neighborhoods.service.ts, apps/web/src/features/admin/neighborhoods/neighborhoods.stories.ts</files>
  <skills_applied>ux-advanced/data-tables-ux (catálogo sobre jx-data-table), product/component-library-governance (jx-neighborhood-row governado), ux-advanced/empty-states-polish (catálogo vazio com CTA), quality/error-ux-patterns (remoção bloqueada cita o bairro; GeoJSON inválido inline), br/ux-copywriting-ptbr, quality/accessibility-pro (badges texto+ícone, textarea mono acessível), ui-ux-pro-max (mono em coordenadas/GeoJSON; botão "Adicionar" sóbrio, não persimmon)</skills_applied>
  <action>
    Catálogo conforme UI-SPEC §3.2–3.4. Usa `jx-data-table` (Task 1) com colunas Nome (· selo "informal") · Polígono (status badge texto+ícone+cor: "Polígono definido" `--success` / "Por nome" `--text-muted` / "Revisar coordenadas" `--error`) · Ações ("Editar polígono" · "Remover" outline `--error`).
    `jx-neighborhood-row`: linha governada; status de polígono; ação remover.
    **Adicionar:** rótulo "Adicionar bairro" + `jx-field` texto (placeholder "Nome do bairro") + botão "Adicionar" (neutro escuro sóbrio, NÃO persimmon). Após adicionar, opcionalmente abre `<textarea>` mono `font.size.xs` para colar **GeoJSON Polygon** (label "Polígono (GeoJSON ou coordenadas) — opcional"); valida sintaxe no blur (faixa lng −180..180, lat −90..90), mostra erro inline "GeoJSON inválido. Cole um Polygon com pares lng,lat." (mapeia 422 do backend Plan 02). Bairro sem polígono é válido (status "Por nome", não é erro).
    **Remover:** com entregas ativas → `jx-error-state` `role="alert"` com a mensagem do backend (409): `Não é possível remover "{Centro}": há entregas ativas nesse bairro. Arquive primeiro.` Sem entregas → confirmação leve. Microcopy `<small>` "Remover bairro com entregas ativas não é permitido — arquive primeiro.".
    **Vazio:** `jx-empty-state` "Nenhum bairro cadastrado ainda." + "Adicione o primeiro bairro da {área}..." + CTA "Adicionar bairro".
    `neighborhoods.service.ts`: HttpClient CRUD `/v1/neighborhoods`. Stories: com-bairros, vazio, remoção-bloqueada, com-polígono/sem-polígono/informal/geojson-inválido · claro+dark.
  </action>
  <verify>
    <automated>cd apps/web && npm run test -- --include='**/neighborhoods*.spec.ts' --watch=false 2>/dev/null || npm run build</automated>
  </verify>
  <done>Catálogo lista sobre jx-data-table; adicionar bairro (com/sem polígono GeoJSON validado); remoção bloqueada cita o bairro (role=alert); estado vazio com CTA; badges texto+ícone; zero #hex; AA claro+dark; build/teste verde.</done>
</task>

</tasks>

## Skills Consultadas
- `ui-ux-pro-max` — editorial-tecnica: R$/m/s/% e GeoJSON/coordenadas em mono; Fraunces italic em 1 palavra do H1 (area); persimmon unica cor de acao; anti AI-slop (sem gradiente/glow/toggle neon/mapa decorativo); densidade de painel admin sobria.
- `quality/accessibility-pro` — `jx-data-table` acessivel (`<th scope>`, `<caption>`, `aria-sort` + botao real, teclado); foco visivel `--focus-ring`; status por texto+icone (nunca so cor); AA claro+dark.
- `product/component-library-governance` — `jx-data-table` e `jx-neighborhood-row` governados (story + baseline S10); generaliza a fila KYC da Phase 5.
- `ux-advanced/design-tokens-system` — so camada semantica (`var(--surface)`, `var(--brand)`); nenhum primitivo nem #hex; nenhuma var nova (UI-SPEC S9).
- `ux-advanced/empty-states-polish` — `jx-empty-state` catalogo vazio com CTA "Adicionar bairro".
- `br/ux-copywriting-ptbr` — sentence case; CTA verbo+objeto sem ponto; erro = o que houve + o que fazer.
- `ux-advanced/form-ux-mastery` — validacao no blur, um erro por campo via `aria-describedby`, CTA habilita so com form valido, confirmacao antes de salvar config sensivel.
- `quality/error-ux-patterns` — `jx-error-state` role=alert para remocao bloqueada (cita o bairro) e falha de salvar; GeoJSON invalido inline.
- `ux-advanced/saas-dashboard-patterns` — config como layout sidebar denso; fieldsets; confirmacao de acao sensivel (before->after) -> audit.
- `ux-advanced/data-tables-ux` — `jx-data-table`: header sticky, aria-sort, zebra, densidade compacta, acoes >=44px, estados loading/empty/error embutidos; catalogo sem paginacao falsa.
- `br/brazilian-forms` — mascara monetaria pt-BR `R$ 0,00` (inputmode decimal), inputmode numeric para m/s/%, NUNCA type=number cru para dinheiro; GeoJSON em textarea mono.
- `ux-advanced/responsive-breakpoint-strategy` — admin desktop-first (tabela densa, 2 col em fieldsets largos).
- `ux-advanced/dark-mode-theming` (DEC-001) — AA nos dois temas; fundos `_bg` claros viram `--surface-elevated` + cor viva no dark.
- `product/visual-regression-testing` — stories/baseline claro+dark dos novos componentes/telas (UI-SPEC S10).

## Skills Dispensadas (com justificativa)
- `ux-advanced/gesture-touch-patterns` — superficie admin e web desktop (mouse/teclado); gesture/touch e da Plan 05 (entregador mobile). Alvos >=44px ainda respeitados via accessibility-pro.
- `ux-advanced/file-upload-ux` — poligono entra por GeoJSON em `<textarea>`, sem upload de arquivo.
- `ux-advanced/onboarding-patterns` / `ux-advanced/trust-safety-ux` — sem signup/onboarding novo; admin ja autenticado (Phase 3).
- `domain/safe2pay-escrow-br` / `domain/saas-billing-canonical` / `ux-advanced/payment-checkout-ux` — has_payments:false; pisos sao guard-rail, nao checkout.
- `mobile/*` / `ux-advanced/chat-ux-patterns` / `ux-advanced/search-filter-ux` — sem mobile/chat/busca nesta tela (catalogo pequeno, sem paginacao/filtro no M1).
- `br/lgpd-compliance` — has_pii:false.

## Performance budget
- LCP < 2500ms / INP < 200ms / CLS < 0.1 / bundle main <= 400KB gz (config.json performance_budget).
- Rotas lazy (Angular). `jx-data-table` sem virtualizacao no M1 (catalogo pequeno); skeleton evita CLS.
- Nenhum #hex (build de tokens). Sem mapa (Phase 9) — sem custo de tiles aqui.

## Integration contracts
- N/A (integration_check:false). Consome `/v1/areas/{id}` e `/v1/neighborhoods` (Plans 01/02) — contrato por tipos de resposta, nao por integration-checker.

## Tech debt deste plano
- Refator da fila KYC (Phase 5) sobre `jx-data-table` e DESEJAVEL; se nao couber no orcamento, registrar SUG (nao bloqueia). Sem nova TD obrigatoria.


<security_validation_notes>
## Security & Validation Notes (frontend — defesa em profundidade; backend é a autoridade)
2. **RBAC de área:** a UI só chama endpoints area-scoped; recurso de outra área retorna 404 (não renderiza estado de "existe mas negado") — não vaza existência.
1. **GeoJSON:** validação de sintaxe/faixa no cliente é UX (feedback rápido); a validação AUTORITATIVA (anti-DoS, self-intersection, vértices) é server-side (Plan 02). A UI nunca confia só no cliente.
> has_pii:false. Threat model pesado N/A. Integration contracts N/A (integration_check:false).
</security_validation_notes>

<observability_checklist>
## Observability checklist (frontend)
- Erros de API (422/409/5xx) capturados e exibidos via `jx-error-state` com copy acionável; `request_id` do erro NÃO exibido ao usuário (logado no console/telemetria de front se houver).
- Confirmação de salvar config sensível deixa claro que a ação é auditada (a trilha real é do backend — Plan 01).
</observability_checklist>

<error_ux_checklist>
## Error UX checklist (UI)
- Erro de faixa (config) → mensagem cita a faixa ("entre 30 e 300 metros"); campo `aria-invalid`.
- GeoJSON inválido → inline "GeoJSON inválido. Cole um Polygon com pares lng,lat." (role inline no jx-field).
- Remoção bloqueada → `jx-error-state` `role="alert"` citando o bairro ("Não é possível remover \"{Centro}\"...").
- Falha ao salvar config → `jx-error-state` `role="alert"` + retry.
</error_ux_checklist>

<verification>
- `cd apps/web && npm run build` → verde (lazy por rota).
- `grep -r "#E84E1B\|#FAF6EE\|#[0-9A-Fa-f]\{6\}" apps/web/src/features/admin/area-config apps/web/src/features/admin/neighborhoods apps/web/src/shared/components/data-table --include=*.scss | grep -v tokens` → 0 ocorrências (Gate 2).
- axe sem violações críticas em config e catálogo (verificação ROADMAP), claro+dark.
- Wireframe-contract de 21 (e 17/18 via migração para jx-data-table) coberto.
</verification>

<success_criteria>
- Config da área (tela 21A) com máscara monetária, ranges, confirmação sensível (REQ-002).
- jx-data-table primitivo governado com stories/baseline.
- Catálogo de bairros (tela 21B) CRUD com polígono GeoJSON opcional e remoção bloqueada (REQ-003).
- Gate 2: zero #hex; AA claro+dark (DEC-001).
</success_criteria>

<output>
After completion, create `.planning/phases/06-rea-oper-vel-bairros-config-cobertura-e-tabela-de-frete/06-04-SUMMARY.md`
</output>
